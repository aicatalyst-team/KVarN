apiVersion: v1
kind: Service
metadata:
  name: vllm-kvarn
  namespace: poc-kvarn
  labels:
    app: vllm-kvarn
    autopoc.io/project: kvarn
spec:
  type: ClusterIP
  selector:
    app: vllm-kvarn
  ports:
    - port: 8000
      targetPort: 8000
      name: http
